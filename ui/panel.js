'use strict';

export default class Panel extends React.Component {
  constructor(props) {
    super(props);
    this.state = {}; //window.addEventListener('meshTransformed', e => this.handleMeshTransformed(e));
  }

  render() {
    return null;
  }

}